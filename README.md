## Installation
1) clone into folder
2) cd to src dir
3) run npm install
4) run gulp watch

## To use

Work in src/components dir by creating new components and mounting them into MainFrame component


